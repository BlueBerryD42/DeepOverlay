{
    "name": "deep-overlay-extension",
    "version": "1.0.0",
    "description": "Chrome extension for DeepOverlay",
    "scripts": {
        "build": "node build-config.js",
        "build:options": "vite build",
        "dev:options": "vite build --watch",
        "build:all": "npm run build && npm run build:options"
    },
    "dependencies": {
        "dotenv": "^16.3.1"
    },
    "devDependencies": {
        "@crxjs/vite-plugin": "^2.3.0",
        "vite": "^7.3.1"
    }
}
